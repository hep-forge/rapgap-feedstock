c_stdlib:
  - sysroot                    # [linux]
  - macosx_deployment_target   # [osx]

c_stdlib_version:
  - 2.17  # [linux]
  - 11.0  # [osx]

root:
  # 6.34 dropped in turn: rivet<4 (3.1.11) now also publishes against
  # root638 (both arches), so rapgap moves onto the same line as the rest
  # of the channel's non-legacy packages -- lets it coexist with
  # root-plus/escalade/etc. in one environment.
  - 6.38

packages:
  - numpy
